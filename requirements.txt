pyOpenSSL>=23.0.0        # TLS support for Twisted / SSL websockets
autobahn>=0.17.2         # Minimum required for websocket engine (see autobahn_check)
simplejson>=3.17.0       # Faster JSON (fallback to stdlib possible)
Twisted>=22.10.0         # Core event framework

attrs>=19.2.0            # Twisted dependency
Automat>=0.8.0           # Twisted state machines
constantly>=15.1.0       # Twisted symbols
hyperlink>=21.0.0        # URL handling
incremental>=21.3.0      # Versioning helper
six>=1.15.0              # Py2/3 compatibility layer still used by Twisted
txaio>=22.2.1            # Autobahn/Twisted abstraction
zope.interface>=5.4.0    # Interface declarations for Twisted
